## Steps to deploy k8s on vagrant VMs

1. Install virtualbox 
2. Install Vagrant
3. Install Vagrant plugin
    . vagrant-disksize
    . vagrant-vbguest
    . trigger

4. Git clone this repo
5. cd vagrant-install && ./run_deploy.sh
